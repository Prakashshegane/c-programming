//using get(str) we can read the value and puts(str ) we can display the string 
#include<stdio.h>
int main()
{
    char str[20];
    printf("enter the string : \n");
    gets(str);
    printf("the string is :\n");
    puts(str);
} 